# Copy to .env and fill in your own keys. Both keys can also be entered or
# changed from the in-app Settings page; .env is only used to seed first run.
# Jobs are selected at runtime from the UI — not configured here.

POLYMER_API_KEY=your_polymer_bearer_token
ANTHROPIC_API_KEY=your_anthropic_key_here
PORT=3001
